# Local Private Files

Recommended local-only paths:

```text
private/
secrets/
subscriptions/*.yaml
subscriptions/*.yml
subscriptions/*.txt
subscriptions/*.list
subscriptions/*.conf
```

These paths are ignored by `.gitignore`.

Use them only on your local machine and do not force-add them unless the content is fully sanitized.
