# OpenWrt Clash Notes

For OpenWrt, keep real subscription URLs and node data outside this public repository.

Recommended approach:

1. Store public templates in this repository.
2. Store private runtime configuration on the router only.
3. Back up private files to a private repository or local encrypted storage, not here.

Common related directories may vary by client/plugin, so verify your actual OpenWrt package before copying files.
