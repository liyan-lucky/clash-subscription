# Template Policy

All committed Clash examples must be sanitized.

Use placeholder values:

```yaml
server: example.com
password: CHANGE_ME
url: YOUR_SUBSCRIPTION_URL
```

Do not use real node data or account data.
