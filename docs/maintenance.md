# Maintenance

Recommended maintenance tasks:

- Keep templates sanitized.
- Review `.gitignore` when new private file types are used.
- Keep the `backup` branch updated before large changes.
- Review workflow warnings before merging changes.
