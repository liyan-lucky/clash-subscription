# Subscriptions

This directory is intentionally protected by `.gitignore`.

Do not commit real subscription files or URLs.

Safe files:

- `.gitkeep`
- sanitized examples only

Private local files such as `.yaml`, `.yml`, `.txt`, `.list`, and `.conf` under this directory are ignored by default.
