# GitHub Workflows

Current workflows:

- `security-check.yml`: checks for obvious sensitive proxy/subscription patterns.
- `backup-branch.yml`: manual reminder workflow for backup branch maintenance.
