# Templates

This directory stores sanitized Clash configuration templates.

Use placeholders instead of private values:

- `example.com`
- `CHANGE_ME`
- `YOUR_SUBSCRIPTION_URL`

Do not commit real subscription files here.
